import { BUDGET, saveData} from "./budget.js";
//localStorage.removeItem("budgetLocal");
let budget = new BUDGET;

//set event listeners
//document.getElementById("save").addEventListener("click", saveData);
